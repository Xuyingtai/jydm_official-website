# 图片使用代码示例

## 📋 快速参考

### 方式一：使用 public 目录（最简单）

**步骤：**
1. 将图片放入 `public/images/cases/case-1.jpg`
2. 在代码中使用：`image: '/images/cases/case-1.jpg'`

### 方式二：使用 assets 目录（需要导入）

**步骤：**
1. 将图片放入 `src/assets/images/logo.png`
2. 在文件顶部导入：`import logo from '@/assets/images/logo.png'`
3. 在代码中使用：`<img src={logo} alt="Logo" />`

---

## 🔧 具体修改示例

### 示例1：修改首页案例图片（Home.tsx）

**当前代码：**
```tsx
const cases = [
  {
    title: '我与宪法-郑州市统计局政策',
    category: '二维Flash+MG动画',
    image: '/api/placeholder/400/300'  // ← 这里需要修改
  },
  // ...
]
```

**修改后：**
```tsx
const cases = [
  {
    title: '我与宪法-郑州市统计局政策',
    category: '二维Flash+MG动画',
    image: '/images/cases/case-宪法统计.jpg'  // ← 改为实际图片路径
  },
  {
    title: 'VR360全景视频拍摄-印尼美娜多的日出横屏',
    category: 'VR360全景视频',
    image: '/images/cases/case-vr360-美娜多.jpg'
  },
  // ... 其他案例类似修改
]
```

### 示例2：修改案例展示页面（Cases.tsx）

**当前代码：**
```tsx
const allCases = [
  {
    id: 1,
    title: '我与宪法-郑州市统计局政策',
    category: '二维Flash+MG动画',
    description: '...',
    image: '/api/placeholder/600/400'  // ← 这里需要修改
  },
  // ...
]
```

**修改后：**
```tsx
const allCases = [
  {
    id: 1,
    title: '我与宪法-郑州市统计局政策',
    category: '二维Flash+MG动画',
    description: '...',
    image: '/images/cases/case-宪法统计.jpg'  // ← 改为实际图片路径
  },
  // ... 其他案例类似修改
]
```

### 示例3：添加Logo（Navbar.tsx）

**方式一：使用 public 目录**
```tsx
<Link to="/" className="navbar-logo">
  <img src="/images/logo/logo.png" alt="吉音动漫" />
  <h1>吉音动漫</h1>
</Link>
```

**方式二：使用 assets 目录**
```tsx
import logo from '@/assets/images/logo.png'

// 在组件中使用
<Link to="/" className="navbar-logo">
  <img src={logo} alt="吉音动漫" />
  <h1>吉音动漫</h1>
</Link>
```

### 示例4：添加老子爷爷形象（Original.tsx）

**当前代码：**
```tsx
<div className="laozi-image">
  <div className="placeholder-image">老子爷爷形象</div>
</div>
```

**修改后（使用 public 目录）：**
```tsx
<div className="laozi-image">
  <img src="/images/laozi/laozi-character.png" alt="老子爷爷" />
</div>
```

**或者使用 assets 目录：**
```tsx
import laoziImage from '@/assets/images/laozi-character.png'

// 在组件中使用
<div className="laozi-image">
  <img src={laoziImage} alt="老子爷爷" />
</div>
```

---

## 📁 推荐的图片组织结构

```
public/images/
├── cases/
│   ├── case-1-宪法统计.jpg
│   ├── case-2-vr360-美娜多.jpg
│   ├── case-3-国家电网.jpg
│   └── ...
├── logo/
│   ├── logo.png
│   ├── logo-white.png
│   └── logo-icon.png
├── laozi/
│   ├── laozi-character.png
│   ├── laozi-award-1.jpg
│   └── ...
└── partners/
    ├── partner-1.png
    ├── partner-2.png
    └── ...
```

---

## ⚠️ 重要提示

1. **路径必须以 `/` 开头**：使用 public 目录时，路径从网站根目录开始
2. **文件名建议使用英文**：避免中文文件名可能导致的编码问题
3. **图片格式**：JPG用于照片，PNG用于Logo和透明背景图片
4. **文件大小**：建议单张图片不超过500KB，上传前先压缩

---

## 🚀 批量替换技巧

如果您有很多图片需要替换，可以：

1. **一次性上传所有图片**到对应目录
2. **使用查找替换功能**（Ctrl+H）：
   - 查找：`/api/placeholder/400/300`
   - 替换为：`/images/cases/case-1.jpg`（然后逐个修改文件名）

或者告诉我您的图片文件名，我可以帮您批量修改代码！

